# BK Traders site structure

- `index.php` ? page shell and include order
- `includes/` ? one file per page section
- `assets/css/styles.css` ? extracted existing styles
- `assets/js/site.js` ? extracted existing behavior
- `images/` ? local copies of former base64 images
- `_old/bktraders_home_v34.html` ? untouched original
